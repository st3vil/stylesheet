LandSty: | # js jigup
    my $t = 'Stylands';
    n NetJig=>W=>{the_W=>$t}
    #n NetJig=>W=>{the_W=>'Stylewholes'}
    #n NetJig=>W=>{the_W=>'Stylephone'}
    return
    my $bb = &s{
        my $C = G&Ct,['toNetJig',1,{s=>$s}];
        Rw JaBabz $C;
        c&s
    };
    
    my $c;
    c.eat = [
        map { G&Ct:["try out  $t",'',{},{e=>$_}]; }
            $bb->(<<''),
                G&c:'::'+C.t;
            $bb->(<<''),
                c$ = ["","",{"W":"Opper"},{"z":[["Fish","",{"el":"1","lv":"blank/neu/Fish"},{}]]}];
                A = A[5];
                G&om,c;
    ];
    
    G&elph,NetPut=>[out=>1=>$c]
    
Stylands: | # reading bay/el/Figures/1,2 and etc
    u jc
    n \'of:om'
    
    lv 34:
        A.b.copy ||= {};
        c$ = A.b.copy;
        if (!c&seeks) {
            t$ = s+'/7';
            delete c[t];
            c&seeks = [t];
        }
        t$
        $.each(c&seeks, &i,v{
            c[v] and return
            t ||= v
        });
        if (t) {
            $.ajax('shed/'+t).done(&L{
                $l = c[t] = G&readLines,L;
                l = l.reverse()[0];
                ls&parent and c&seeks.push(s+'/h/'+ls&parent);
                G&throat,7
            });
            return T.not = '2';
        }
        $.each(c&seeks.reverse(), &i,v{
            $N = c[v];
            $.each(N.reverse(), &i,v{
            
            });
        });
        
        
        ~wlv: s, C
        console.log('gotlv::',C,c);
        
        
    
    #c Lines - should Wunify remote file seeings
    
    n \'of:i'
    u j
    readLines:
        $L = s.split("\n");
        $tot = L.length;
        $N = [];
        $in = [];
        $.parseJSON(e)
        
        try{
        while (L.length) {
        
        $l = L.shift();
        $lin = l.match(/^(\s*)(.*)$/);
        $in = lin[1];
        $d = in.length / 2;
        l = lin[2];
        $li = l.split("\t");
        $t = li[0];
        $y = li[1];
        $c = li[2];
        $sc = li[3];
        
        t = djson t if t.match(/^\W/);
        
        !y.length && y = 1
        y = 1*('0.'+y);
        
        if (s.match(/^\W/)) {
            # fifth thing would be c.s
            s = djson s;
        }
        if (c.match(/^"/)) {//"
            s = djson c;
            c = {};
        }
        else if (c.match(/^\W/)) {
            c = djson c
        }
        else if (c) {
            c = G&peel,c
        }
        else { c = {} }
        
        s and c.s = s;
        
        if (sc.match(/^\W/)) {
            sc = djson sc
        }
        elsif (sc) {
            sc = G&peel,sc
        }
        else { sc = {} }
        
        $C = {t:t,y:{cv:y},c:c,sc:sc};
        
        # add blockquoted c/sc
        $k
        $v
        $indenting = &{
            $m = L[0].match("^"+in+"  (\w[\S ]+):( (\||.+))?$");
            !m && return;
            k = m[1];
            v = m[3];
            return 1;
        };
        while (indenting()) {
            $string
            if (v === '|') {
                string++;
                undef $v;
            }
            L.shift;
            k = k.split(' ');
            $nk = k.shift();
            $gk = k.join(' ');
            if (!v) {
                $s = [];
                $one;
                $blockquoting = &{
                    $m = L[0].match("^"+in+"    (.*)$");
                    !m && return;
                    one = m[1];
                    return 1;
                }
                while (blockquoting()) {
                    s.push(one);
                    L.shift();
                }
                $v = join "\n", @s;
                sayyl "Loaded til $l[0]\n with $nk $gk - ".slim(30,$v);
            }
            unless ($string) {
                $v = Load jsyaml.load(s);
            }
            else {
                $v .= "\n" unless $v =~ /\n$/s;
            }
            
            $C.>$nk.>$gk = $v;
        }
        
        in[d] = C;
        if (d) {
            $p = in[d-1];
            ps&z ||= [];
            ps&z.push(C);
        }
        else {
            N.push(C);
        }
        
        }
        }
        catch (e) {
            throw ["On line "+(tot - L.length)+": "+l, e];
        }
        return N
        
        // copy Line, use for Load:
        return jsyaml.load(s);
ach: | # inhale Lines
    my @l = ref $s ? @$s : split "\n", ($s || ''.`cat $f`);
    my $tot = @l;
    my $N = [];
    local A.V = 0;
    my @in;
    my $l;
    my $peel = &s{
        return {map { split':',$_ } split ',', $s}
    };
    eval{
    while (@l) {
        $l = shift @l;
        my ($in) = $1 if $l =~ s/^(\s+)//;
        my $d = length($in) / 2;
        my ($t,$y,$c,$sc,$s) = split "\t", $l;
        
        sayyl "$in $l";
            
        $t = djson $t if $t =~ /^\W/;
        
        $y = 1 if !length$y;
        $y = 0+('0.'.$y);
        
        if ($s =~ /^\W/) {
            # fifth thing would be c.s
            $s = djson $s;
        }
        if ($c =~ /^"/) {
            $s = djson $c;
            $c = {};
        }
        elsif ($c =~ /^\W/) {
            $c = djson $c
        }
        elsif ($c) {
            $c = $peel->($c);
        }
        else { $c = {} }
        c.s = $s if $s;
        
        if ($sc =~ /^\W/) {
            $sc = djson $sc
        }
        elsif ($sc) {
            $sc = $peel->($sc)
        }
        else { $sc = {} }
        
        my $C = {t=>$t,y=>{cv=>$y},c=>$c,sc=>$sc};
        
        # add blockquoted c/sc
        while ($l[0] =~ /^$in  (\w[\S ]+):( (\||.+))?$/) {
            my ($k,$v) = ($1,$3);
            my $string;
            if ($v eq '|') {
                $string++;
                undef $v;
            }
            shift @l;
            my ($nk,$gk) = split ' ', $k, 2;
            sayyl "$in C.t C.y.cv - $nk $gk ($string) $v";
            unless ($v) {
                saybl $in."Beaming in";
                my @s;
                while ($l[0] =~ /^$in    (.*)$/) {
                    push @s, $1;
                    shift @l;
                }
                $v = join "\n", @s;
                sayyl "Loaded til $l[0]\n with $nk $gk - ".slim(30,$v);
            }
            unless ($string) {
                $v = Load $v
            }
            else {
                $v .= "\n" unless $v =~ /\n$/s;
            }
            
            $C.>$nk.>$gk = $v;
        }
        
        saygr "Popped $in".ki$C;
        $in[$d] = $C;
        if ($d) {
            my $p = $in[$d-1];
            push @{ps&z||=[]}, $C;
        }
        else {
            push @$N, $C;
        }
    }
    };
    $@ && die "On line ".($tot - @l).": $l\n\n$@";
    $N

